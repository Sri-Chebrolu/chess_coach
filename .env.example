ANTHROPIC_API_KEY=your_anthropic_api_key_here
# STOCKFISH_PATH is pre-set in the Dockerfile (/usr/games/stockfish).
# Override only if running outside Docker with a custom binary location.
# STOCKFISH_PATH=/usr/local/bin/stockfish
